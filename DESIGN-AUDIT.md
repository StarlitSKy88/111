# Ma 极简主义设计规范梳理 + 现状审计

## 一、我们到底想要什么

### 核心理念
- **Ma（間）**：意义存在于事物*之间*的空间，空白不是缺失
- **侘寂**：不完美、无常、不完整中的美
- **余白**：慷慨边距的艺术
- **不北欧**：更安静、更不对称、更有耐心

### "不要 PPT 感" = 避免什么
1. ❌ 大色块、对称布局、居中堆叠
2. ❌ 大量 logo 墙、合作伙伴展示
3. ❌ 三栏卡片矩阵
4. ❌ 每页都"塞满"内容
5. ❌ 鲜艳色块 / 渐变卡片 / 圆角胶囊
6. ❌ 数据仪表盘式的密集信息
7. ❌ 通用 SaaS 模板

### Ma 的"高级感"从哪里来
1. ✅ 不对称布局，刻意留白
2. ✅ 巨大单一排版元素（>9vw 标题）
3. ✅ 慢速、淡入、单向动画
4. ✅ 朱红 #C0392B 每页 ≤1 次
5. ✅ 文字层级替代装饰元素
6. ✅ 微弱噪点纹理
7. ✅ 几乎不可见的 1px 阴影

---

## 二、当前前端 vs Ma 规范审计

### 🔴 graph.html（刚刚重做）— 严重违规

| 违规项 | 当前实现 | Ma 要求 |
|:---|:---|:---|
| 装饰性粒子 | 50 个浮动彩色粒子 | **禁止装饰性插图** |
| 渐变填充 | `url(#nodeGradient)` 径向渐变 | **禁止任何形式的渐变** |
| 发光滤镜 | `filter: drop-shadow` + 高斯模糊 | **禁止带可见色调的投影** |
| 玻璃拟态 | `backdrop-filter: blur(20px)` | **无玻璃拟态** |
| 多色高亮 | 5 色粒子（红/蓝/紫/青/橙） | **任何屏最多 2 色** |
| 强调色滥用 | 朱红 + 蓝 + 紫 + 青 | **每页最多 1 次朱红** |
| 弹簧式动效 | 节点放大 1.5x 悬停 | **仅透明度偏移** |
| 频繁小动效 | requestAnimationFrame 持续滚动 | **最少 400ms** |

**问题本质**：graph.html 当前是"暗色赛博朋克 SaaS"，完全不是 Ma 极简主义。

### 🟡 index.html（首页）— 部分合规

| 维度 | 现状 | 评分 |
|:---|:---|:---:|
| 暗色模式配色 | `#111110` / `#F0EDE6` / `#C0392B` | ✅ |
| 字体配对 | Noto Serif JP + Noto Sans SC | ✅ |
| 排版层级 | clamp 字号系统 | ✅ |
| 间距系统 | 8px 倍数 | ✅ |
| 导航结构 | 固定顶栏 + 1px 描边 | ✅ |
| CTA 样式 | 填充按钮 + 朱红 | ⚠️ 像 SaaS 模板 |
| Hero 留白 | 50% 屏幕空白 | ✅ 但**无视觉锚点** |
| 不对称布局 | 大部分居中对称 | 🔴 |
| 超大数字 | 缺失 | 🔴 |
| 区段分隔 | 横线 + 简单布局 | ⚠️ |
| 朱红使用频率 | 每屏 3-5 处 | 🔴 违反规则 |

### 🟢 admin.html — 不在本次范围

### 🟢 节点详情页（56 个）— 大体合规
- 已有 Ma 风格：衬线标题、克制留白、1px 描边
- 缺配图、缺 TOC 视觉引导（部分节点有）

---

## 三、我的建议

### 核心原则：**克制** + **不对称** + **慢速**

#### ✅ 应该做的（Ma 风格增强）

1. **超大数字锚点** — 立即摆脱 PPT 感
   ```
   在每个区段角落放巨大数字（如 "01" "02"），20vw 字号，3% 透明度
   这是"高端杂志"而非"PPT"的关键差异
   ```

2. **不对称区段** — 立即摆脱对称感
   ```
   Hero 标题：margin-left: 12vw（偏左），右侧刻意的空白
   内容：grid-template-columns: 3fr 2fr（不等列）
   ```

3. **收紧配色** — 立即高级感
   ```
   全文只用 1 种强调色（朱红），多色全删
   多色会立刻变成 SaaS，1 色才是编辑感
   ```

4. **巨大排版作为视觉** — 替代装饰图
   ```
   Hero 标题字号 clamp(5rem, 12vw, 12rem)
   一个标题就是一件艺术品，不需要装饰图衬托
   ```

5. **微噪点纹理** — 增加质感而非装饰
   ```
   SVG 噪点 overlay，opacity 0.025
   这会让暗色背景有"和纸"触感
   ```

#### ❌ 不应该做的（避免 PPT/装饰感）

1. **不要在 Hero 加大图** — 装饰图是 SaaS 思路
2. **不要做 logo 墙 / 客户案例展示**
3. **不要做 3 列卡片矩阵**
4. **不要用渐变 / 发光 / 粒子 / 模糊**
5. **不要快速动效（<400ms）**
6. **不要圆角按钮**
7. **不要多色强调**

#### 🎨 mmx 的正确用法

mmx 图像生成在 Ma 框架下应该**极少使用**：
- ❌ 不要生成 Hero 大图（违反"无装饰插图"）
- ❌ 不要生成赛博朋克背景图（违反"柔和留白"）
- ❌ 不要生成插图卡片（违反"无装饰插图包"）
- ✅ 仅生成 1-2 张**写实摄影**（如 老人手作陶器 / 京都雨后石板路 / 毛笔字特写）作为"区段装饰"
- ✅ 仅作为"垂直留白间隙的呼吸点"，每页最多 1 张

#### 📐 更推荐的资产策略

由于 Ma 强调"排版是主要视觉元素"：
1. **mmx text chat** 生成 1 段有诗意的中文标语（替代"探索图谱"按钮文字）
2. **mmx image** 仅生成 1 张"印章式"的站点 logo 升级版（取代文字 logo）
3. **手写 SVG 几何符号**替代图标（更 Ma）
4. **CSS @keyframes** 慢速淡入（800ms 缓动）

---

## 四、graph.html 修复方案

既然 graph.html 是最近大改的，但**完全违反 Ma 规范**，建议**回退到 Ma 风格**：

### 关键改动清单

| 改动 | 当前 | 目标 |
|:---|:---|:---|
| 粒子背景 | 50 个彩色粒子 | **删除**，用 1 层 SVG 噪点替代 |
| 节点渐变 | 径向渐变填充 | **纯色填充** `#1A1A18` |
| 节点发光 | drop-shadow + 高斯模糊 | **删除**，无阴影 |
| 玻璃拟态 | 导航/侧栏 backdrop-blur | **删除**，纯色背景 |
| 多色高亮 | 5 色粒子 | **删除**，只用 1 色 |
| 朱红强调 | 多处使用 | **仅 1 处**（关键提示） |
| 节点悬停 | 放大 1.5x + 缩放 | **仅描边变色**，无缩放 |
| 动画时长 | 200ms 渐变 | **600-800ms** |
| 自动滚动 | 持续 requestAnimationFrame | **删除**或减速到 0.1px/帧 |
| 连线 | 渐变 + 发光 | **1px 纯色**细线 |

### 目标视觉
- **极简**：节点是黑底白边的圆圈，朱红仅用在"已订阅/已读"等关键状态
- **编辑感**：像在看一本暗色主题的高质量杂志的目录页
- **慢速**：所有 hover 仅透明度变化 600ms
- **呼吸感**：节点之间大量空白，标题区段超大

---

## 五、最终推荐

| 选项 | 描述 | 适合 |
|:---|:---|:---|
| A. 严格 Ma | 完全遵守规范，去掉所有装饰 | 极致品味/编辑感 |
| B. Ma + 1 张图 | 严格 Ma，但在首页放 1 张写实摄影 | 平衡审美与生动 |
| C. 折衷版 | Ma 风格为主，1 处微发光（如节点悬停） | 保留一点现代感 |

**我推荐 B**——既保持 Ma 哲学的克制，又用 1 张高品质写实摄影作为视觉锚点，避免空洞感。

---

## 评审问题

1. **方向选择**：A / B / C 哪个更符合昴君期望？
2. **graph.html 处理**：是否同意回退到 Ma 风格（删掉当前霓虹风）？

---

## 六、昴君决策（2026-06-01）

**选择 C 折衷版**：
- ✅ 基础设计元素（颜色、字体、色调、线框、动效）严格 Ma
- ✅ 首页**用知识图谱方式呈现**，让用户快速了解网站
- ✅ "眼前一亮"通过**创意排版 + 知识图谱可视化**实现，**不是发光/霓虹**
- ❌ 不要粒子、发光、玻璃拟态、彩色高亮

---

## 七、C 方案具体执行方向

### 7.1 基础设计系统（严格 Ma）— 适用于所有页面

| 维度 | 规范 |
|:---|:---|
| 配色 | 主 `#111110` / 文 `#F0EDE6` / 次 `#7A7670` / 强调朱红 `#C0392B` |
| 强调色 | 每页最多 1 处使用朱红 |
| 字体 | 标题 Noto Serif JP / 正文 Noto Sans SC / 标注 Geist Mono |
| 圆角 | `border-radius: 0` 或最大 2px |
| 阴影 | 几乎不可见 `0 1px 3px rgba(0,0,0,0.04)` |
| 边框 | 仅 1px 实线 `#2A2A28` |
| 间距 | 8px 倍数，区段间 ≥96px |
| 段落宽度 | `max-w-2xl` / `max-width: 58ch` |
| 动效时长 | ≥400ms，缓动 `cubic-bezier(0.25, 0.46, 0.45, 0.94)` |
| 悬停效果 | 仅透明度偏移 / 描边变色，无缩放 |
| 装饰 | 无 Emoji / 无渐变 / 无玻璃拟态 / 无粒子 |

### 7.2 首页 index.html "知识图谱" 创意方案

**目标**：3 秒内传达"57 个创业节点 · 一人公司路径"

**方案 A：SVG 微缩图谱（推荐）**
- Hero 区右侧用 SVG 绘制一张"家谱式"知识图谱
- 57 个节点用带编号的小圆点表示（无填充，1px 描边）
- 连线 1px 实线，关键节点用朱红描边
- 像一张抽象的"创业路径图"
- 用户悬停某个节点时可看到名称

**方案 B：排版瀑布流（备选）**
- 不用图，用超大数字+文字构建
- 例如：
  ```
  01 想法 → 02 验证 → 03 原型 → ...
  ```
- 7 个阶段每个一行，巨大数字 + 阶段名 + 简短说明
- 像高级杂志的"目录页"

**方案 C：图谱式 TOC（结合）**
- Hero 区用大号衬线数字列出 7 个阶段，每个阶段下挂 4-13 个节点名
- 像一本书的目录，但用**网格 + 极细线**连接
- 没有实际图，就是"排版版的图谱"

**推荐：方案 A（SVG 微缩图谱）** — 既 Ma 又生动，是最佳平衡

### 7.3 首页具体设计要素

```
┌──────────────────────────────────────────────────────┐
│  OPC 节点百科                                          │  ← 顶部导航
├──────────────────────────────────────────────────────┤
│                                                      │
│  OPC 创业路径              1    2    3               │  ← Hero 左：超大排版
│                              \  |  /                  │  ← Hero 右：微缩图谱
│  57 个节点                      ●                    │     （SVG 绘制）
│  一人公司的完整创业导航           /|\                  │
│                                 4 ● 5                 │
│                                                      │
│  探索图谱 →                                          │  ← 极简 CTA
│                                                      │
│   01                                                 │  ← 超大数字锚点
│  ─────────                                           │
│                                                      │
│  完整节点导航                                        │  ← 不对称区段
│                                                      │
│  按阶段分组，点击任意节点获取攻略。                  │
│  [知识图谱 →]                                        │
│                                                      │
│   02                                                 │  ← 下一区段
│  ─────────                                           │
│                                                      │
│  节点列表（按阶段）                                  │
│  产品验证 · 环境搭建 · 核心开发 · ...                 │
│                                                      │
└──────────────────────────────────────────────────────┘
```

### 7.4 节点列表"图谱感"实现

不用传统 3 列卡片，改为：

```html
<!-- 阶段标题（大号衬线） -->
<h2 class="stage-title">产品验证</h2>
<p class="stage-subtitle">01 · 从想法到 MVP</p>

<!-- 节点列表（极简文字 + 编号） -->
<ol class="node-list">
  <li>
    <span class="node-num">01</span>
    <a href="...">个人能力与资源盘点</a>
  </li>
  <li>...</li>
</ol>
```

样式：
- 节点编号 24px 衬线，朱红
- 节点名 16px 无衬线，行高 1.8
- 列表项之间 1px `#2A2A28` 极细分隔
- 悬停时整行向右平移 8px（600ms 缓动）

### 7.5 mmx 在 C 方案中的真实使用

mmx 在严格 Ma 框架下使用价值有限：

| mmx 调用 | 必要性 | 优先级 |
|:---|:---:|:---:|
| ❌ Hero 大图 | 不需要 | — |
| ❌ 阶段图标 | 不用图标，用文字 | — |
| ❌ 装饰插图 | Ma 禁止装饰 | — |
| ✅ Hero 区"知识图谱"的节点分布参考（看几张参考图找灵感） | 可选 | 低 |
| ✅ OG 分享卡（暗色背景 + 大字标题） | 需要 | 中 |
| ✅ 1 张 印章式 logo（替代文字 logo） | 可选 | 低 |

**推荐 mmx 用法**：
1. 1 次 `mmx search` 找"知识图谱"+"极简"+"日式"参考
2. 1 次 `mmx image generate` 生成站点 OG 分享图
3. **不再生成其他图片**

---

## 八、执行步骤（按顺序）

### Step 1：index.html 首页改造（核心）
- 配色/字体/间距严格按照 Ma 规范
- Hero 加 SVG 微缩知识图谱（不用图片）
- 加超大数字锚点（01、02 飘在区段角落）
- 节点列表改为"目录式"流式布局
- CTA 改为"下划线 + 文字"形式

### Step 2：graph.html 回退到 Ma 风格
- 删除粒子、渐变、发光、玻璃拟态
- 节点改为纯色填充 + 1px 描边
- 节点悬停仅描边变朱红（无放大）
- 连线 1px 纯色
- 自动滚动减速到 0.1px/帧 或删除

### Step 3：节点详情页一致性（不修改内容）
- 仅在 CSS 变量层统一用 Ma 规范
- 不破坏现有节点内容

### Step 4：生成 OG 图
- 用 mmx 生成 1 张 OG 分享图
- 暗色背景 + 巨大"OPC 节点百科"标题

---

## 九、graph.html Ma 规范回归修复（2026-06-01）

昴君反馈："你的修改没有遵循之前的我们的设计规范"。检查 GSAP 集成改动后，识别出 3 类违反 Ma 规范的问题并修复：

### 9.1 违反项与修复

| 违反项 | 原实现 | 修复后 | Ma 规范来源 |
|:---|:---|:---|:---|
| **悬停缩放** | `magneticHover` 用 `attr: { r: baseR * 1.18 }` 节点放大 1.18x | **完全删除** `magneticHover` / `magneticLeave` 函数及调用 | 6.2 动效原则："仅透明度偏移，无缩放" |
| **朱红滥用** | 14 处使用 `var(--accent)`（hover/focus/modal-id/tooltip-id/logo-mark 等） | 缩减到 5 处，仅保留**数据状态**：关键节点、跨阶段连线、搜索框 focus、主 CTA | 3.1 调色板："每页最多 2 种颜色…每页朱红最多 1 个元素" |
| **过渡过快** | tooltip 400ms / zoom 按钮 400ms | 全部统一为 600ms | 7 动效原则："时长最少 600ms，偏好 600-800ms" |
| **弹簧缓动** | GSAP `power3.out` / `power2.out` | 改用 `sine.out` / `sine.inOut`（更接近 `cubic-bezier(0.25, 0.46, 0.45, 0.94)`） | 7 动效原则："无弹跳、无弹簧" |

### 9.2 保留的"非典型"朱红用法

为维持图谱可视化的功能性，保留 5 处朱红（均为**数据状态**，非装饰）：

| 位置 | 用途 | 类别 |
|:---|:---|:---|
| `.node-circle.key` | 6 个关键节点（1/11/22/30/43/57）实心红 | 节点数据状态 |
| `.link.cross-stage` | 跨阶段连线（7 条关键跃迁） | 关系数据状态 |
| `.search-panel:focus-within` | 搜索框激活状态 | 输入状态 |
| `.modal-link.accent` | "进入节点 →" 主操作 CTA | 单一主操作 |

### 9.3 保留的 GSAP 方案（符合 Ma）

| 方案 | 状态 | 理由 |
|:---|:---|:---|
| Phase 1: 中心开花入场 | ✅ 保留 | 仅 opacity + y 偏移（16px），无缩放 |
| Phase 2: ScrambleText 数字滚动 | ✅ 保留 | 仅 textContent 变化，无视觉变形 |
| Phase 2: 镜头飞越切换阶段 | ✅ 保留 | D3 transform 镜头，无元素缩放 |
| **Phase 2: 磁性悬停** | ❌ **删除** | `r: baseR * 1.18` 缩放违反规范 |

### 9.4 最终合规清单

- [x] 无渐变（`gradient` 0 处）
- [x] 无背景模糊（`backdrop-filter` 0 处）
- [x] 无发光投影（`drop-shadow` / `filter:` 0 处）
- [x] 无装饰粒子
- [x] 无元素缩放（仅 D3 镜头 transform）
- [x] 朱红仅用于数据状态（5 处）
- [x] 所有过渡 ≥600ms
- [x] 缓动为 `sine.out` / `cubic-bezier(0.25, 0.46, 0.45, 0.94)`
- [x] 尊重 `prefers-reduced-motion`
- [x] 排版作为主要视觉（`.col-header-bg` 72px 巨号锚点）

---

## 十、graph.html 知识图谱重构（2026-06-01）

昴君反馈："你还是没有正视'知识图谱'这个概念"。检查原实现发现是 7 列 Gantt 视图，**不是知识图谱**。重做。

### 10.1 方案确定（AskUserQuestion）

| 问题 | 选择 |
|:---|:---|
| 布局 | **FORCE + TREE 双布局**（可切换） |
| 边 | **跨阶段用朱红曲线**（二次贝塞尔） |
| 节点尺寸 | **小节点 6-9px**，仅关键节点放大 |

### 10.2 FORCE 布局（力导向）

**实现要点**：
- `d3.forceSimulation` + `forceLink` + `forceManyBody(-220)` + `forceCollide(28)` + `forceStage()` 自定义 + `forceCenter`
- 同阶段内边 `strength=0.7, distance=70`
- 跨阶段关键边 `strength=0.25, distance=200`
- 让同阶段聚集，跨阶段跨越

**视觉**：
- 节点 6-9px 圆圈，1px `#F0EDE6` 描边
- 6 个关键节点（1/11/22/30/43/57）实心朱红 + 8px
- 跨阶段 7 条关键跃迁：朱红 1px 二次贝塞尔
- 阶段标签自然散布在聚类中心

### 10.3 TREE 布局（阶段列）

**实现要点**：
- **放弃 d3.tree**（所有叶子同 y 坐标会挤一行）
- 改为手工按列分布：每阶段一列，节点按 id 在列内垂直堆叠
- `leftPad=260, rightPad=260` 让开左右侧栏
- `rowGap` 动态计算 = min(48, availableH / maxNodesPerStage)
- `node.fx/fy` 钉死位置（force simulation 已停）

**视觉**：
- 7 个垂直列等距分布
- 每列顶部：`STAGE 0X` + 阶段名 + `N NODES`
- 节点按 id 顺序向下排列
- 跨阶段朱红贝塞尔曲线在列间弧形跨越

### 10.4 双布局切换实现

**核心难点**：D3 力导向会把 `link.source` 从 id 字符串替换为节点对象引用，TREE 模式保持 id 字符串。

**解决方案**：
- 新增 `linkEndId(endpoint)` helper 统一处理两种引用形式
- 修复 5 处使用 `d.source.id` 的地方（applyFocus、showTooltip、ticked、dragged、animateEntrance）
- `ticked()` 中根据 `typeof d.source === 'object'` 分别取 x/y
- `applyForceLayout()` 进入时 `n.fx=null; n.fy=null;` 清除 pin

**切换动画**：
- 切换后 200ms（tree）/ 1500ms（force）调 `fitToScreen()`
- fitToScreen 用 800ms d3 transition 平滑变换 transform
- FORCE 阶段分批渐入（GSAP timeline 180ms cascade）

### 10.5 Ma 规范合规

| 维度 | 状态 |
|:---|:---:|
| 配色 | ✅ 仅主色 + 朱红（关键状态） |
| 字体 | ✅ Noto Serif JP 标题 / Geist Mono 标注 |
| 圆角 | ✅ `border-radius: 0` |
| 阴影 | ✅ 无可见阴影 |
| 边框 | ✅ 1px `#2A2A28` |
| 装饰 | ✅ 无粒子、无渐变、无玻璃拟态 |
| 动效 | ✅ 全部 ≥600ms（fitToScreen 800ms / hover 600ms） |
| 缓动 | ✅ `cubic-bezier(0.25, 0.46, 0.45, 94)` |
| 朱红 | ✅ 仅 5 处（关键节点/跨阶段边/搜索focus/主CTA） |
| 不对称 | ✅ 阶段列 TREE 不等距高，FORCE 自然聚类 |
| 超大元素 | ✅ "57" 背景锚点 20vw |

### 10.6 关键文件改动

| 文件 | 变更 |
|:---|:---|
| `graph.html` | `applyTreeLayout` 重写为列式布局 |
| `graph.html` | 新增 `linkEndId()` helper |
| `graph.html` | `ticked()` 兼容对象/id 两种端点 |
| `graph.html` | `fitToScreen()` 800ms 缓动 |
| `graph.html` | 双布局切换 UI（FORCE/TREE 按钮） |

---

## 评审问题（最终版）

1. **首页"知识图谱"方案**：A（SVG 微缩图谱）/ B（排版瀑布流）/ C（图谱式 TOC）选哪个？
2. **节点列表样式**：是否同意"目录式"流式布局（不用 3 列卡片）？
3. **mmx 使用范围**：是否同意**仅生成 OG 图**（1 次调用）？其他靠排版解决？
4. **graph.html 回退**：确认删除当前霓虹风，回归 Ma？

确认后蕾姆开始执行。
3. **mmx 真实使用价值**：在 Ma 框架下 mmx 只能生成 1-2 张资产，是否仍想使用？
4. **首页空白问题**：是否接受用"超大数字锚点"解决，而非装饰图？

确认后蕾姆开始执行。
