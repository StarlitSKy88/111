{
  "id": "task-006",
  "type": "feature",
  "title": "节点列表页组件",
  "description": "创建 NodeList 组件，显示所有 42 个节点\n\n功能需求：\n1. 页面加载时调用 GET /api/nodes 获取所有节点\n2. 显示节点卡片网格：ID、标题、难度、分类\n3. 点击卡片跳转到 /editor/:slug\n4. Japanese Ma 风格：暗色系 #111110，左对齐，极简\n\n组件结构 src/pages/NodeList.jsx：\n```jsx\nimport { useEffect, useState } from 'react'\nimport { useNavigate } from 'react-router-dom'\n\nexport default function NodeList() {\n  const [nodes, setNodes] = useState([])\n  const [loading, setLoading] = useState(true)\n  const navigate = useNavigate()\n\n  useEffect(() => {\n    fetch('/api/nodes')\n      .then(res => res.json())\n      .then(data => {\n        setNodes(data.data || [])\n        setLoading(false)\n      })\n  }, [])\n\n  if (loading) {\n    return (\n      <div className=\"flex items-center justify-center min-h-screen\">\n        <div className=\"text-[#F0EDE6]\">加载中...</div>\n      </div>\n    )\n  }\n\n  return (\n    <div className=\"p-8\">\n      <h1 className=\"text-2xl font-light mb-8 tracking-tight\">节点列表</h1>\n      <div className=\"grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-px bg-[#2A2A28] border border-[#2A2A28]\">\n        {nodes.map(node => (\n          <div\n            key={node.id}\n            className=\"bg-[#111110] p-6 cursor-pointer hover:bg-[#1A1A18] transition-colors\"\n            onClick={() => navigate(`/editor/${node.slug}`)}\n          >\n            <div className=\"flex justify-between items-start mb-4\">\n              <span className=\"text-[#C0392B] text-xs tracking-widest\">\n                {String(node.id).padStart(2, '0')}\n              </span>\n              <span className=\"text-[#8A8A85] text-xs\">{node.difficulty}</span>\n            </div>\n            <h2 className=\"text-lg font-light mb-2 text-[#F0EDE6]\">{node.title}</h2>\n            <p className=\"text-sm text-[#8A8A85] line-clamp-2\">{node.summary}</p>\n          </div>\n        ))}\n      </div>\n    </div>\n  )\n}\n```",
  "verification": {
    "type": "api",
    "command": "curl http://localhost:3001/api/nodes | jq '.data | length'",
    "expected": "返回节点数量约 42"
  },
  "files": ["admin-app/src/pages/NodeList.jsx"],
  "status": "pending",
  "dependencies": ["task-005"]
}